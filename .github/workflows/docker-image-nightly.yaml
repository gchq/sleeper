name: Docker Image Tests Nightly
on:
  workflow_dispatch: # Allow manual running
  schedule:
    - cron: '0 3 * * *'

jobs:
  build:
    uses: ./.github/workflows/docker-image-all.yaml
    with:
      testsDockerOptions: --cache-to type=gha,mode=max
